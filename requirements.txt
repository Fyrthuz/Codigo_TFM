torch>=2.5.0
torchvision>=0.20.0
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0
scikit-learn>=1.3.0
pandas>=2.0.0
Pillow>=10.0.0
tqdm>=4.65.0
scikit-image>=0.21.0
imageio>=2.28.0
medpy>=0.4.0
opencv-python>=4.8.0
seaborn>=0.12.0
PyYAML>=6.0
ttach>=0.0.3
kagglehub
nibabel>=5.0.0

# External models (install separately as needed):
# git+https://github.com/JJGO/UniverSeg.git  # UniVerSeg few-shot 2D

# Note: pydensecrf is NOT compatible with Python 3.14 (Cython build fails).
#       CRF post-processing is disabled automatically when pydensecrf is missing.
#       To use CRF, create a venv with Python ≤3.11 and install:
#         pip install pydensecrf
